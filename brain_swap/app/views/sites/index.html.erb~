<h1>Brain Swap</h1>

<table >
  <tr>
    <th>Title</th>
    <th>Beschreibung</th>
    <th>Category</th>
    <th>Host</th>
    <th></th>
    <th></th>
  </tr>

<% @sites.each do |site| %>
  <tr class="<%=cycle('rowa','rowb')%>">
    <td class="rowa"><%=link_to site.name, site.linkws %></td>
    <td><%= site.content %></td>
    <td><%= site.category %></td>
    <td><%= site.host %></td>
    <td><%= link_to 'Show', site %></td>
    <td><%= link_to 'Edit', edit_site_path(site) %></td>
    <td><%= link_to 'Destroy', site, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Site', new_site_path %>
